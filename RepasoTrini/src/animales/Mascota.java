package animales;

public interface Mascota {
    // no tiene implementación de codigo ni atributo.

    void mostrarEdad(int edad); //entre parentesis lo que va a recibir
    int calcularEdadHumanos(int edad);
}
